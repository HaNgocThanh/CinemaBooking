namespace CinemaBooking.Domain.Enums;

/// <summary>
/// Loại ghế trong phòng chiếu (template).
/// </summary>
public enum SeatType
{
    /// <summary>Ghế thường (hàng A, B).</summary>
    Regular = 0,

    /// <summary>Ghế VIP (hàng C, D).</summary>
    VIP = 1
}
